# netlimit —— 本机全网出向限速（与 HAProxy / LVS 无关的独立工具）。
#
# 一句话：把**这台机器发出去的全部流量**合起来限在一个总额度内，不管它
# 从哪张网卡出去、源地址是哪个 IP。
#
# 与本仓库另一套东西（rl_limiter，给 HAProxy 的入口限速）的关系：**没有
# 关系**，不共享任何代码与配置，可以单独拿走。共同点只是都用内核 tc，
# 以及从那边用真实事故换来的两条硬规则（tc 的 ID 按十六进制、HTB 的
# cburst 必须显式给，见 plan.py 模块头）。
