# 周期性重新下发限速。
#
# 为什么需要它——一次性下发有两个盖不住的洞：
#
#   1. **后插的网卡不会被限住。** 热插网卡、云上加弹性网卡、某张网卡从
#      down 变 up，都会多出一条完全绕过限速的出口。而这时 IFB 上的队列树
#      看起来一切正常，`status` 也报"限速在跑"——**看起来限着，其实漏了
#      一整张网卡**，这是本方案最危险的失效方式。
#   2. 有人手工 `tc qdisc del` 了，或别的工具重建了根 qdisc。
#
# netlimit apply 是幂等的：已经一致时**一条命令都不发**（不会每分钟重建
# 一次队列树、抖一次流量），所以让它频繁跑没有代价。
[Unit]
Description=周期性核对本机全网限速（补上后插的网卡、纠正被手工改动的规则）

[Timer]
OnBootSec=2min
OnUnitActiveSec=1min
AccuracySec=10s

[Install]
WantedBy=timers.target
